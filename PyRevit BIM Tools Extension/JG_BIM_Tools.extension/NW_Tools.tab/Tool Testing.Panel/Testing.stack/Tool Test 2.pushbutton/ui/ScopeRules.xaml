<UserControl
    x:Class="ScopeRules"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Scope Rules" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Rules}"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="0,0,0,6" Background="#FFF3F3F3" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="{Binding [name], FallbackValue=Rule}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding [description], FallbackValue=—}" Foreground="Gray"/>
                            <WrapPanel Margin="0,4,0,0">
                                <TextBlock Text="Categories: " FontWeight="SemiBold"/>
                                <ItemsControl ItemsSource="{Binding [categories]}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="3" Padding="2,0" Margin="4,0,0,0" Background="#FFEAEAEA">
                                                <TextBlock Text="{Binding}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
