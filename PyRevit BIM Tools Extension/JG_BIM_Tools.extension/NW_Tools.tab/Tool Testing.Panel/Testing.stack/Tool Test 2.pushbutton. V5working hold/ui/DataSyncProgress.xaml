<UserControl
    x:Class="DataSyncProgress"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="mc">

    <Border Padding="8" Background="#FFF8F8F8" CornerRadius="6">
        <StackPanel>
            <TextBlock Text="{Binding Status}" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <ProgressBar Minimum="0"
                         Maximum="{Binding Total, FallbackValue=1}"
                         Value="{Binding Done}"
                         Height="14"/>
            <DockPanel Margin="0,6,0,0">
                <TextBlock DockPanel.Dock="Left">
                    <Run Text="{Binding Done}"/><Run Text="/" /><Run Text="{Binding Total}"/>
                </TextBlock>
                <TextBlock DockPanel.Dock="Right" TextAlignment="Right">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0:P0}">
                            <Binding Path="Done"/>
                            <Binding Path="Total"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </DockPanel>
        </StackPanel>
    </Border>
</UserControl>
